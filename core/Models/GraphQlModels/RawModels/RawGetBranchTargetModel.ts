/**
 * Represents the raw get branch target model that is unchanged from the GraphQL query.
 */
export interface RawGetBranchTargetModel {
	/**
	 * Gets or sets the oid of the target.
	 */
	oid: string;
}
